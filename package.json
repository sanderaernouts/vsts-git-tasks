{
  "name": "vsts-git-tasks",
  "version": "0.0.1",
  "description": "Git related tasks for VSTS",
  "main": "index.js",
  "scripts": {
    "bash": "bash -c",
    "clean": "npm run bash \"find -maxdepth 1 -type d -name _build -exec rm -r {} \\;\"",
    "compile": "tsc",
    "copy": "npm run bash \"find tasks/ -not -type d -not -name *.ts -print0 | xargs -0 -I{} cp --parents {} ./_build && cp README.md _build && cp extension-icon.png _build && cp vss-extension.json _build\"",
    "build": "npm run clean && npm run compile && npm run copy",
    "test": "npm run build && node \"_build/tests/createpullrequest/test.js\""
  },
  "author": "Sander Aernouts",
  "license": "ISC",
  "dependencies": {
    "npm": "^6.1.0",
    "vso-node-api": "^6.5.0",
    "vsts-task-lib": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^10.5.2"
  }
}
